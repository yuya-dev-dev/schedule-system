# 開発ロードマップ

## 開発方針

現行業務の課題、要件、業務フロー、画面、DB設計を整理した後、動作する小さな単位で実装とテストを繰り返す。初期MVPを一度に作り切るのではなく、一覧と入力がつながる最小の縦切りから段階的に完成させる。

ポートフォリオでは、次の流れを説明できる状態を目指す。

1. 現行業務の課題整理
2. 顧客ヒアリングと要件定義
3. 業務フロー、画面、DBの初期設計
4. MVP範囲とリスクのレビュー
5. 小さな機能単位での実装とテスト
6. 利用者による試運転と改善

## ブランチ運用

- mainブランチへ直接変更しない
- 作業単位でブランチを作成する
- Pull Requestで差分を確認してからmainへ取り込む
- ドキュメント単独の変更は専用PRに分ける
- 機能実装と、その機能を守る単体・結合テストは原則として同じPRに含める
- 未テストの機能実装だけをmainへ取り込まない

ブランチ名の例:

- docs/phase0-review-fixes
- chore/spring-boot-setup
- feat/schedule-list
- feat/request-drafts
- test/domain-rules

## フェーズ0: 上流工程整理

目的:

- 実装前に作るものの範囲を明確にする
- MVPとMVP後の機能を分ける
- 文書間の矛盾と実装リスクをレビューする

成果物:

- README
- 要件定義
- 要件ヒアリング議事録
- 業務フロー
- 画面一覧
- DB設計
- 開発ロードマップ
- テスト方針

完了条件:

- 改訂版MVPについて顧客とベンダの認識が一致している
- 要件、業務フロー、画面、DB、テスト方針に重大な矛盾がない
- 実装前に確認すべきリスクが明示されている

## フェーズ1: 技術検証とプロジェクト土台

目的:

- Java / Spring Bootの最小構成を作る
- DB接続、起動確認、自動テストの土台を整える

想定タスク:

- Spring Bootプロジェクト作成
- パッケージ構成決定
- 開発用DB設定
- 本番想定DBを決定し、H2との差異を踏まえた利用範囲を決める
- 本番相当DBで `DRAFT / PUBLISHED`、時間重複、楽観ロックの小さな同時実行検証を行う
- Thymeleafなどの画面方式と、自動保存の通信方式を決定する
- トランザクション分離レベル、ロック方式、競合エラーの返却方式を決定する
- FlywayなどのDBマイグレーション方針決定
- フォーマット方針決定
- GitHub Actionsの初期設定
- READMEへ起動・テスト手順を追加

完了条件:

- ローカルでアプリが起動できる
- DBへ接続できる
- テストコマンドを実行できる
- CIでビルドとテストが成功する
- 画面方式、自動保存通信、採用DB、同時実行方式の技術判断が記録されている
- 本番相当DBで先着案件だけが `PUBLISHED` になる競合テストが通る
- フェーズ2以降の期間目安を、技術検証結果に基づいて暫定設定できる

## フェーズ2: 最小の縦切り機能

目的:

- 月間一覧、入力フォーム、DB保存を最短経路で接続する
- 早い段階でPCとiPhoneから画面を確認できるようにする

想定タスク:

- 日本時間の現在月を表示する月間一覧
- 水曜日・金曜日、8:30-17:30、30分単位のセル生成
- 前月・当月・翌月タブ
- 空白セルから日付を引き継ぐ新規入力フォーム
- `schedule_requests` のEntity、Repository、Service
- 時間範囲、作業種別、依頼者名の保存
- 作業種別ごとの一覧反映条件と `DRAFT / PUBLISHED` の技術状態
- 入力済みセルから既存案件フォームへの遷移
- 空白セルから入力、保存、一覧反映、再表示までを通すスモークE2Eテスト1本

完了条件:

- URLを開くと現在月が表示される
- 空白セルから案件を入力してDBへ保存できる
- 保存した案件が正しい30分セルへ表示される
- 入力済みセルから同じ案件を開ける
- 最小のスモークE2Eテストが安定して通る

フェーズ2実装結果（2026年6月20日）:

- 日本時間の現在月と前月・翌月タブを実装
- 水曜日・金曜日、8:30-17:30、30分単位の月間一覧を実装
- 空白セルからの日付引き継ぎ、新規保存、一覧反映を実装
- 入力済みセルからの確認・編集を実装
- 依頼者名、作業種別、案件色、継続矢印を実装
- 30分単位の時間入力、隣接時間の登録、時間重複エラーを実装
- PC幅とiPhone幅の表示確認を実施
- 月生成の単体テストと、入力から再表示までのスモークテストを追加
- 実ブラウザE2Eはフェーズ4でPlaywright for Javaを採用し、主要6経路を追加済み

フェーズ2の「一覧へ戻る」は保存を兼ねる暫定操作とし、フォーカス離脱時の自動保存、保存状態表示、下書き再開はフェーズ3A・3Bで完成させる。

## フェーズ3: 改訂版MVPの完成

目的:

- 合意済み入力項目と重要な業務ルールを追加し、試運転可能なMVPを完成させる

### 3A: 入力項目・必須判定・自動保存

- 6種類の作業種別と合意済み入力項目
- 同行あり時の動的項目表示、集合場所・出発時間の条件付き必須判定、任意の使用車両指定
- 同行解除時の集合場所、出発時間、使用車両の削除
- 出庫状態3種類の入力
- フォーカス離脱時と戻る操作時の自動保存
- 保存中、保存済み、保存失敗、再試行の表示
- フォーム上部の対象日表示、赤字の必須・エラー表示
- 作業種別変更時の詳細削除確認

完了条件:

- 作業種別ごとの必須・任意・条件付き必須が動作する
- 自動保存に成功・失敗した状態を利用者が判断できる
- 機能実装と対応する単体・結合テストが同じPRで通る

### 3B: 下書き・重複・同時操作・削除

- 月間一覧上部の下書き一覧、再開、削除
- 下書き理由と競合時間帯の表示
- 作業日を過ぎた下書きの自動物理削除
- 下書きから通常案件へ移る際の重複再確認
- 同時登録の先着優先と具体的な競合エラー
- 同一案件更新時の楽観ロック
- 二重確認付きの依頼キャンセルと物理削除

完了条件:

- 下書き、時間重複、同時登録、楽観ロック、物理削除の主要テストが通る
- 先着案件だけが `PUBLISHED` となり、後続入力を理由付き `DRAFT` として保持できる
- 機能実装と対応する単体・結合テストが同じPRで通る

### 3C: 一覧表示・過去日・端末確認

- 過去日の閲覧専用制御
- 依頼者名、作業種別、矢印、案件色、`＊未入力` の表示
- 開始時間順に5色を割り当てる表示制御
- iPhoneでの一覧・案件詳細閲覧、ノートPCでの基本操作確認

完了条件:

- セル表示、色、矢印、`＊未入力`、過去日制御が要件どおり動作する
- PCで主要操作、iPhoneで一覧と案件詳細の閲覧を確認できる
- 機能実装と対応するテストが同じPRで通る

TDD対象:

- 終了時間が開始時間より後であること
- 時刻が30分単位かつ8:30-17:30内であること
- 作業種別ごとの一覧反映条件がそろうまで `DRAFT` とすること
- 時間重複した入力を理由付きの `DRAFT` として保持すること
- 下書きが時間枠を確保しないこと
- 日本時間で作業日を過ぎた下書きだけを自動削除すること
- 通常案件へ移る際に重複を拒否すること
- 同時登録では先着案件だけが `PUBLISHED` となり、後続が理由付き `DRAFT` になること
- 同行あり時に集合場所と出発時間だけが必須になること
- 同行解除時に3項目の保存値を削除すること
- 入庫、商品管理では依頼者名を任意とし、時間範囲と作業種別だけで不足警告を出さないこと

完了条件:

- 改訂版MVPの機能要件と主要テストがすべて通る
- PCで一覧、入力、編集、下書き、キャンセルを確認でき、iPhoneで一覧と案件詳細を閲覧できる
- 初期MVPに含めない機能が混入していない

## フェーズ4: 品質向上と内部総合試験

目的:

- ポートフォリオとして説明でき、初回導入候補機能の検討へ進める品質に整える
- 配送・設置担当者とCodexで、機能の正しさを可能な限り網羅して確認する

想定タスク:

- リスク、テストケースID、環境、合格基準を定めたフェーズ4テスト設計
- 自動テスト拡充
- 主要ブラウザ操作のE2Eテスト
- エラーメッセージと例外処理の整理
- 架空サンプルデータ整備
- READMEのセットアップ手順更新
- 画面キャプチャと構成図追加
- 要件ID、画面、DB、テストのトレーサビリティ整理
- 架空データだけを使う一時共有環境の構築
- PC、iPhone、社用スマートフォンから共有URLへ接続する技術検証
- 複数端末での同時操作、アプリ再起動、DBデータ保持の確認
- 機能不具合、操作性改善、追加要望を分類する

フェーズ4実施状況（2026年6月22日）:

- 固定Clock、テスト専用H2、ケースごとのfixture初期化を持つPlaywright E2E環境を構築
- 新規登録、重複下書き、同時編集、キャンセル競合、過去案件閲覧、自動保存再試行の主要6経路を自動化
- 主要6経路を3回連続で実行し、計18件すべて成功
- E2E-001〜015、通信切断、HTTP 500、応答保留、編集中削除、未入力離脱、保存直後更新を自動化
- blur保存と一覧遷移による二重送信を防ぎ、保存キュー完了後に遷移する制御へ改善
- 自動保存後のURLを既存案件または下書きURLへ更新し、ブラウザ更新時に保存内容を復元できるよう修正
- ブラウザE2E 19件、性能4件、アプリ再起動・H2ファイル保持試験、実AutosaveServiceのPostgreSQL同時登録試験を含む全94件の自動テストに成功
- GitHub ActionsでChromiumを導入して全テストを実行する構成を追加
- 一時H2ファイルへ架空案件を保存してSpring Bootを完全停止・再起動し、案件、一覧・詳細表示、Flyway履歴が保持されることを自動確認
- ノートPC相当の100%・125%表示で月切替、新規入力、自動保存、一覧反映、下書き、スクロールを確認
- iPhone相当幅で直近勤務日の初期表示、横スクロール、固定月タブ、案件詳細の表示を確認
- 個人iPhone Safariからテザリング経由の共有URLへ接続し、一覧、案件詳細、前後月タブを実機確認
- `demo`プロファイル限定で、6種類の作業種別を含む架空案件6件を投入できる構成を追加
- 存在しない案件またはURLに、一覧へ戻れる利用者向け404画面を追加
- 存在しない下書き、キャンセル確認、下書き削除にも同じ404画面を適用
- Windows Maven Wrapperを通常ディレクトリの `.m2` で再現可能に修正
- 50案件、50下書き、同日6案件、10回連続保存の性能・容量試験に成功
- README、システム構成図、実機画面、性能測定結果、要件トレーサビリティを更新
- 共有環境DBの再起動確認は、正式な配置先とDBが未決定のためフェーズ6で実施する

フェーズ4判定: **完了（2026年6月22日）**

共有環境、社用スマートフォン、共有DB再起動は正式配置に依存するため、フェーズ4の未完了ではなくフェーズ6の開始条件として管理する。

完了条件:

- READMEの手順で起動とテストを再現できる
- 架空データだけで主要業務をデモできる
- 重要な業務ルールに自動テストがある
- 架空データ限定のローカル共有URLへ個人PCとiPhoneから接続できる
- ローカルアプリの再起動後もDBデータが保持される
- 配送・設置担当者とCodexで確認可能な重大不具合が残っていない

## フェーズ5: 初回導入機能と全面UI改修

目的:

- MVP完成後、社員へ見せる前に初回導入へ含める追加機能を完成させる
- 追加機能の完成後に全画面のデザインを刷新し、社員試験の二度手間を減らす
- 機能とUIの統合試験を終え、社員へ初めて提示できるリリース候補版を作る

実施順序:

| 区分 | タスク | 推奨思考レベル | 主な進め方 |
| --- | --- | --- | --- |
| 5A | 採用機能、画面遷移、受入条件の最終確認 | 最高 | 顧客とCodexで、実装前に必要な判断だけを確定する |
| 5B | 左上の年月選択と日付操作ルール | 高 | Codexが実装、テスト、PR作成まで一括して進める |
| 5C | 外部祝日カレンダー連携、DBキャッシュ、祝日列の自動除外 | 最高 | Codexが障害時動作を含めて実装、テストする |
| 5D | 日付単位の休み設定、解除、既存案件削除の確認 | 最高 | 破壊的操作の受入条件を確認後、Codexが一括して進める |
| 5E | 案件の別日コピーとコピー先選択画面 | 最高 | コピー対象、選択方法、重複時動作を確認後、Codexが一括して進める |
| 5F | 機能を変えない全画面の全面UIリニューアル | 最高 | 顧客の視覚確認と選択を挟みながら共同で反復する |
| 5G | 統合テスト、文書更新、社員提示用リリース候補版の確定 | 高 | Codexが回帰試験と最終整理を一括して進める |

進捗:

- 5Aは2026年6月22日に完了。5Bから5Eまでの受入条件を確定し、要件、DB方針、画面一覧、テスト方針へ反映した
- 5Bは2026年6月23日に完了。年の数値入力と月選択による任意年月表示、未来月の登録、過去月の閲覧専用化、現在月基準の下部3タブ維持を実装し、自動テストとPC・iPhone幅の画面確認を行った

実行単位:

- 5Bから5Eまでは、既存要件でCodexが単独実行できる範囲を先に完成させる。確認は、外部連携、物理削除、画面遷移など重要な判断に限定する
- 5Bと5C、5Dと5Eは連続して進められるが、機能ごとに独立したPRとし、実装と対応テストを同じPRへ含める
- 5Fは5Bから5Eまでの機能と画面構成が確定してから開始し、追加機能によるデザインの手戻りを避ける
- 5Fと5Gの間では新しい機能を追加せず、社員へ提示する機能範囲を凍結する

進め方:

- 各機能の着手時に、実装コスト、データ削除リスク、受入条件を顧客とCodexで再確認する
- 機能実装と対応する単体・結合・画面テストを同じPRに含める
- 祝日同期は日付列を一覧生成時に除外し、案件データを自動削除しない
- 休み設定は全員が操作可能だが、実運用では配送・設置担当者本人またはセールス部長が行う
- 5Fでは現在の機能、業務ルール、情報量、入力項目を変えず、レイアウト、タイポグラフィ、余白、部品、視覚階層を刷新する
- 5Fのデザインは洗練されたモノトーンを基調とする。ただし、案件識別の5色、エラー、警告、保存状態など意味を持つ色は視認性のため残す
- PCでの入力を主対象とし、iPhoneでは一覧と詳細の閲覧、縦横スクロール、拡大が正常に行えることを維持する
- DOMやCSSの変更による自動保存、セル遷移、動的フォーム、固定表示の回帰を5Gで確認する

完了条件:

- 5Bから5Eまでの追加機能と対応テストが完成している
- 全画面が合意したデザイン方針で統一され、機能や業務ルールが意図せず変わっていない
- PCとiPhoneで主要画面と主要操作を確認できる
- 全自動テスト、主要E2E、手動確認が通る
- 社員へ見せる機能範囲が凍結され、社員提示用リリース候補版を作れる

## フェーズ6: 正式運用準備と正式導入用リリース候補版完成

目的:

- 社員の最終受入試験で問題がなければ、翌日にも正式導入できる状態を先に整える
- 実在案件を入力する前に、配置、情報管理、バックアップ、復旧方法を確認する

想定タスク:

- 正式なアプリとDBの配置先を決定する
- 会社の情報管理方針と共有方法を確認する
- DBバックアップの取得方法と保管先を決定する
- バックアップからの復元手順を作成し、架空データで復元テストを行う
- 誤削除、障害、連絡先を含む最低限の運用手順を作成する
- 個人iPhone、社用スマートフォン、ノートPCでリリース候補版を確認する
- 複数端末で入力、編集、競合、削除、再起動、データ保持を再確認する
- 社員試験で使用する架空データと確認項目を準備する

完了条件:

- 配送・設置担当者とCodexで実行可能な機能・競合・端末テストが完了している
- 会社が配置先と社員試験用の共有方法を確認している
- バックアップ取得と復元テストが成功している
- 障害・誤削除時の最低限の対応手順がある
- 社員試験で重大問題がなければ、正式導入へ移れるリリース候補版になっている

## フェーズ7: 社員最終受入試験と正式導入判断

目的:

- セールス部門の数名に完成したリリース候補版を触ってもらい、説明なしで業務利用できるか確認する
- 機能の網羅的な再テストではなく、現場の使いやすさと導入可否を最終判断する

実施順:

1. 許可を得たセールス部門の数名が架空データで主要操作を行う
2. UIとUX、説明なしで操作できるか、業務上不足する情報がないかを評価する
3. 指摘を導入阻害、導入後改善、追加要望に分類する
4. 導入阻害となる問題だけを修正し、必要箇所を再確認する
5. セールス部長へ試験結果、配置、バックアップ、運用案を説明する
6. 正式導入を承認された場合は、最短で翌日から利用開始できる状態へ移す

完了条件:

- 社員からUIとUXの評価を収集できている
- 正式導入を妨げる重大な問題が残っていない
- セールス部長が正式導入を承認している
- 会社が正式配置先と実データ利用を承認している
- 上記を満たした後にのみ実在案件の入力を開始する

## フェーズ8: 導入後の改善

- 軽微なUI改善、操作性改善、追加要望は正式導入後も継続して対応する
- 実際の利用で判明した問題は、不具合、運用改善、追加機能に分類して優先順位を決める
- 実在データを扱う変更では、バックアップと影響範囲を確認してからリリースする
- 通知、変更履歴、CSV出力、地図、AIアシスタントなどは必要性が確認できた場合だけ検討する

## 将来検討

| 機能 | 方針 |
| --- | --- |
| 通知 | 試運転で一覧の見落としが発生した場合だけ検討する |
| 変更履歴 | 誤操作や追跡の必要性が高まった場合に検討する |
| CSV出力 | 外部共有や集計が必要になった場合に検討する |
| バックアップ・復旧 | MVP外だが、実在案件を入力する前の正式運用ゲートで必ず再評価し、復元テストを行う |
| 地図・ルート最適化 | 配送順の支援が必要になった場合に検討する |
| PWA | ブラウザのショートカットでは不足する場合だけ検討する |
| AIアシスタント | MVP完成後、架空データとローカルAIを優先して使い方Q&Aと確認付き入力支援を試作する。会社承認なしに実データを外部送信しない |

## スプリント運用

- 期間は確約日ではなく、完了条件を伴う暫定目安として扱う
- フェーズ1の技術検証が終わった時点で、フェーズ2以降の見積もりを更新する
- 1スプリントは1週間を目安とし、各スプリントに1〜2日の調査・修正バッファを見込む
- 各スプリントで設計確認、実装、テスト、振り返りを行う
- 1PRは説明しやすい機能単位に分ける
- 機能実装と対応する単体・結合テストは同じPRに含める
- 完了条件をIssueまたはPR本文へ明記する

暫定的な進行イメージ:

| 期間目安 | 主な対象 | 終了条件 |
| --- | --- | --- |
| 1週目 | フェーズ1とフェーズ2 | 技術判断を記録し、最小縦切りとスモークE2Eが通る |
| 2週目 | フェーズ3Aと3Cの一覧基礎 | 入力・自動保存・主要一覧表示が動く |
| 3週目 | フェーズ3B | 下書き・競合・同時操作・削除のテストが通る |
| 4週目 | フェーズ3C残作業とフェーズ4 | 端末確認、品質整理、内部総合試験が終わる |
| 5週目以降 | フェーズ5とフェーズ6 | 初回導入機能を選定・実装し、リリース候補版と正式運用準備を完成させる |
| リリース候補完成後 | フェーズ7とフェーズ8 | 社員最終受入後、承認されれば正式導入し、以後は運用しながら改善する |

この表は学習や技術調査の結果に応じて更新し、日付より各フェーズの完了条件を優先する。

## ポートフォリオで説明する観点

- 現場課題から要件へ落とし込んだ流れ
- レビューでMVPを縮小し、データ消失リスクを抑えた判断
- Excelの一覧性を残しながら詳細情報を構造化した設計
- Java / Spring Bootで層を分けた実装方針
- 自動保存、下書き、同時登録をテストで守る方針
- PR運用と試運転による改善プロセス
