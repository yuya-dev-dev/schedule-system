services:
  schedule-system:
    build: .
    container_name: schedule-system
    ports:
      - "8080:8080"
    environment:
      SCHEDULE_HOLIDAYS_SYNC_ENABLED: "false"
    volumes:
      - ./data:/app/data
